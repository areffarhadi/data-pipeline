# Core Dependencies for DAVA Pipeline
# Generated from dava-env virtual environment

# Deep Learning Framework
torch==2.8.0
torchaudio==2.8.0

# ASR and Speech Processing
whisperx==3.7.4
openai-whisper==20250625
transformers==4.57.1

# Audio Processing
soundfile==0.13.1
pydub==0.25.1

# Speaker Diarization
pyannote.audio==3.4.0
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1

# Utilities
numpy==2.0.2
PyYAML==6.0.3
tqdm==4.67.1
accelerate==1.11.0

# HuggingFace Hub (for model downloads)
huggingface-hub==0.36.0

# Optional: For faster-whisper backend (if needed)
# faster-whisper==1.2.1
# ctranslate2==4.6.0

# Optional: For topic detection with Qwen (install in separate environment)
# torch>=2.0.0
# transformers>=4.30.0

# Note: Some packages may have additional dependencies that will be installed automatically:
# - CUDA libraries (nvidia-*) are typically installed with PyTorch
# - Other dependencies like pandas, scipy, etc. are pulled in by the above packages
